class Validators {
  static bool isValidPassword(String password) {
    /*RegExp exp = new RegExp("[A-Z]+[a-z]*\W*\d+[a-zA-Z]+");
    Iterable<Match> matches = exp.allMatches(password);*/
    return true;
  }
  
}